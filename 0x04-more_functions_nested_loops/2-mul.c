#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiple
 * @y: the second number to multiple
 * Return: the result of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
